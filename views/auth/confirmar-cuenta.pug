extends ../layout/app

block contenido
  .py-10
    h1.text-4xl.my-10.font-extrabold.text-center Bienes
      span.font-normal Raices
    h2.text-center.text-2xl.font-extrabold= pagina

  div(class="bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto")
    p(class= error ? 'bg-red-500' : 'bg-green-500').text-white.uppercase.py-2.px-5.w-full.font-bold.text-center.rounded-md= mensaje
    
    if !error
      a(href="/auth/login" class="text-center font-bold uppercase text-sm text-slate-600 mt-10 block") Ya Puede Iniciar Sesión