extends ../layout/app

block contenido
  .py-10
    h1.text-4xl.my-10.font-extrabold.text-center Bienes
      span.font-normal Raices
    h2.text-center.text-2xl.font-semibold= pagina

    if errores
      div(class="max-w-md mx-auto my-10")
        each error in errores
          p.bg-red-600.text-white.uppercase.text-xs.text-center.p-2.mb-1.font-bold= error.msg

    .mt-8.mx-auto.max-w-lg
      .bg-white.py-8.px-4.shadow.rounded-lg
        form.space-y-7(method="POST" action="/auth/olvide-password" noValidate)
          //- //- input(type="hidden", name="_csrf" value= csrfToken)
          div   
            label.block.text-sm.uppercase.text-gray-500.mb-1.font-bold(for="email") Recupera tu cuenta
            input#email.w-full.px-3.py-2.border.border-gray-300.rounded-md.placeholder-gray-400(placeholder="Ingresa tu email" type="email" name="email" class="focus:outline-none focus:ring-2 focus:ring-yellow-400 focus:border-transparent" value= email ? email : "" )
          .flex.items-center.justify-between
            p.text-gray-500.text-xs ¿No tienes cuenta?
              a.text-gray-500.text-xs.font-semibold(href="/auth/registro")  Registrate
            p.text-gray-500.text-xs ¿Ya tienes cuenta?
              a.text-gray-500.text-xs.font-bold(href="/auth/login")  Inicia Sesión
          input(class="mx-auto text-center px-2 w-fit bg-[#142906] text-white font-bold py-3 cursor-pointer rounded hover:focus:outline-none focus:ring-2 focus:ring-gray-600 focus:ring-opacity-50 transform hover:scale-105" type="submit" value="Enviar email")

