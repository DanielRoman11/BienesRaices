extends layout/app

block contenido
  section.py-5
  h1.text-center.text-4xl.font-extrabold.py-10.pb-5= pagina

  if propiedades.length
    div(class="flex flex-col sm:flex-row flex-wrap items-center gap-x-3")
      each propiedad in propiedades
        include templates/card
  else
    p(class="mx-auto w-full text-center mt-10")
      |  No se encuentran resultados para esta busqueda